// Shortest-word

const findShort = (s) => {
  let strSplit = s.string('')
  let shortestStrLength = s
      for (let i=0; i < s.length; i++) {
        
}
}


// const findShort = (s) => s.split(' ').reduce((a, b) => {
//     return b.length < a.length ? b : a
// }).length