// Return Negative 

const makeNegative = (num) => {
    return (num > 0 ? num * -1 : num)
  }
  
  // const makeNegative = (num) => {
  //   if (num > 0) {
  //     return num * -1
  //   }
  //   return num
  // }
  
  // const makeNegative = (num) => -Math.abs(num)